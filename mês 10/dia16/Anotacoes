#Métodos de manipulação de lista

#append(x)
#Adiciona x ao final da lista

#extend(iterable)
#adciona todos os elementos de iterable ao final da lista

#insert(i, x)
#insere x na posição i

#remove(x)
#remove aa primeirsa ocorrência de x na lista

#pop([i])
#remove e retorna o elemento na posição i (ultimo se não for expecificado)

#index(x[, start[, end]])
#retorna o indece da primeira ocorrência de x no intervalo [start, end]

#count(x)
#retorna o numero de ocorrencias de x na lista

#sort(key=None, reverse=True)
#Ordena a lista in-place, key é uma função de comparação, reverse se True inverte a ordem.

#reverse()
#Inverte a ordem dos elementos na lista

#slice[start:end:step]
#acessa uma parte da liosta com um intervalo definido